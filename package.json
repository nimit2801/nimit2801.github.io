{
  "name": "nimit2801.github.io",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 8080",
    "host": "vite --host 8080",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --port 8080",
    "addCname": "cd dist && touch CNAME && echo nimitsavant.me >> CNAME && cd ..",
    "deploy": "yarn addCname && gh-pages -d dist -m \"New Push from main ✨\""
  },
  "dependencies": {
    "simple-analytics-vue": "^3.0.2",
    "vue": "^3.3.4",
    "vue-plausible": "^1.3.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.2.0",
    "gh-pages": "^5.0.0",
    "typescript": "^4.8.4",
    "vite": "^3.2.2",
    "vue-tsc": "^1.0.9"
  }
}
